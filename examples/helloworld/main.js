// require React components used
var React = require('react');
var ReactDOM = require('react-dom');

// render a h1 tag into html element with id="example"
ReactDOM.render(
  <h1>Hello, world!</h1>,
  document.getElementById('example')
);
